{
  "name": "clipboard-manager-extension",
  "version": "1.0.0",
  "description": "Clipboard Manager Browser Extension",
  "scripts": {
    "package": "node package-extension.js && node generate-update-manifest.js",
    "deploy": "npm run package && git add -A && git commit -m \"Update extension package\" && git push",
    "build": "echo 'Static site - copying files' && cp -r . ../build || xcopy /E /I /Y . ..\\build\\ || echo 'Files ready'"
  },
  "devDependencies": {
    "archiver": "^6.0.2"
  }
}
