{
  "name": "clipboard-manager-extension",
  "version": "1.0.0",
  "description": "Clipboard Manager Browser Extension",
  "scripts": {
    "icons": "node generate-icons.js",
    "package": "npm run icons && node package-extension.js && node generate-update-manifest.js",
    "deploy": "npm run package && git add -A && git commit -m \"Update extension package\" && git push"
  },
  "devDependencies": {
    "archiver": "^6.0.2",
    "sharp": "^0.34.5"
  }
}
